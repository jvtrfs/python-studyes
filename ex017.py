'''import math
ca = float(input('qual o tamanho do cateto oposto: '))
co = float(input('qual o tamanho do cateto adjacente: '))
hi = math.hypot(ca, co)
print('a hipotenusa mede {:.2f}'.format(hi))'''

# podemos fazer importar diretamente a hypot da biblioteca math para facilitar ainda mais, segue abaixo.

# importando a hypot em math, o codigo fica mais limpo e bonito